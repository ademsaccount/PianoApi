spring:
  application:
    name: piano-api

  datasource:
    url: jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres
    username: postgres.chipbbxegptuteoryuqj
    password: ${DB_PASS:1252365896321aA.}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baselineOnMigrate: true

supabase:
  jwt:
    issuer: https://chipbbxegptuteoryuqj.supabase.co/auth/v1
    audience: authenticated

server:
  port: ${PORT:8080}
